# core.autocrlf is true on this machine, so anything Git checks out gets CRLF
# by default. A shell script with CRLF line endings dies on a fresh clone with
# "bad interpreter: /bin/sh^M", which would silently disable the pre-commit
# collision check — the one thing it must not do.
.githooks/* text eol=lf
